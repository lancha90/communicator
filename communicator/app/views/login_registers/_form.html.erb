<%= form_for(@login_register) do |f| %>
  <% if @login_register.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_register.errors.count, "error") %> prohibited this login_register from being saved:</h2>

      <ul>
      <% @login_register.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user %><br />
    <%= f.text_field :user %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
