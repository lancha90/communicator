<h1>Listing login_registers</h1>

<table>
  <tr>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @login_registers.each do |login_register| %>
  <tr>
    <td><%= login_register.user %></td>
    <td><%= link_to 'Show', login_register %></td>
    <td><%= link_to 'Edit', edit_login_register_path(login_register) %></td>
    <td><%= link_to 'Destroy', login_register, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Login register', new_login_register_path %>
