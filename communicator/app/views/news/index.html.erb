<h1>Listing news</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Description</th>
    <th>Student</th>
    <th>Teacher</th>
    <th>Admin</th>
    <th>Validity</th>
    <th>dependence</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @news.each do |news| %>
  <tr>
    <td><%= news.title %></td>
    <td><%= news.description %></td>
    <td><%= news.student %></td>
    <td><%= news.teacher %></td>
    <td><%= news.admin %></td>
    <td><%= news.validity %></td>
    <td><%= news.dependence %></td>
    <td><%= link_to 'Show', news %></td>
    <td><%= link_to 'Edit', edit_news_path(news) %></td>
    <td><%= link_to 'Destroy', news, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New News', new_news_path %>
