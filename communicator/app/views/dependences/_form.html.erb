<%= form_for(@dependence) do |f| %>
  <% if @dependence.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dependence.errors.count, "error") %> prohibited this dependence from being saved:</h2>

      <ul>
      <% @dependence.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :mision %><br />
    <%= f.text_area :mision %>
  </div>
  <div class="field">
    <%= f.label :vision %><br />
    <%= f.text_area :vision %>
  </div>
  <div class="field">
    <%= f.label :information %><br />
    <%= f.text_area :information %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
